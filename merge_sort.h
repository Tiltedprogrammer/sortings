#pragma once

void merge_sort(char** array, int size);
void merge_sort_recurse(char** array1, char** arra2, int size, int* flag);
void merge(char** array1, int amount1, char** array2, int amount2, char** rezult_array);

